package com.AaronGoidel.mka.project_euler.sums;

/**
 * Created by agoidel2019 on 11/17/16.
 */
public class AlgebraWay
{

    int limit = 100;
    long addSquares = (long) Math.pow(limit * (limit + 1) / 2, 2);
    long sqareSum = limit * (limit + 1) * (2 * limit + 1) / 6;
}
